#比較
if (条件式) 文 {
    Trueのときの処理;
}

# 論理演算子
- &&
 - AND
- ||
 - OR
- !
 - NOT

# 3つ以上の場合分け
if (条件式) {
    真の場合に実行する文; 
} else if (条件式) {
    真の場合に実行する文; 
} else {
    偽の場合に実行する文 ; 
    }
}

# 変数と整数値の比較
switch (条件式) {
    case 数値:
        実行文;
        break;
    case 数値:
        実行文;
        break;
    default:
        実行文;
        break;
}